{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Name pool",
  "description": "A list of given names, family names, or nicknames for one region and kind. The file name = the pool's id, referenced by the region",
  "type": "object",
  "required": ["id", "kind", "values"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$"
    },
    "kind": {
      "enum": ["given", "family", "nick"]
    },
    "values": {
      "type": "array",
      "minItems": 8,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 2
      }
    }
  }
}
