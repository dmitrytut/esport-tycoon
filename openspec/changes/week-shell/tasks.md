## 1. Browser package and trusted run inputs

- [x] 1.1 After PR-1 merges, rebase PR-2 on current `origin/master`; rerun `openspec list --json` and `openspec show season-contest --json --deltas-only` if #45 remains active. Keep every #45 declared path, especially `docs/INDEX.md` and core exports, untouched. Verify the PR-2 diff contains only the proposal's `Affects` paths.
- [x] 1.2 Create `packages/shell/package.json`, `index.html`, `vite.config.ts`, `src/main.ts` and a shell-only DOM `tsconfig.json`; add root `pnpm dev`, shell typecheck and production build to `pnpm verify`, keep the root core/tool `tsconfig.json` free of browser globals, and add only needed import-boundary rules in `eslint.config.mjs`. Run the existing content validator before dev/build and verify `pnpm dev` opens a page and `pnpm --filter @et/shell build` succeeds with no Node-only module in the browser output.
- [x] 1.3 Implement `packages/shell/src/content.ts` using the shipped JSON and existing schemas, validate required shapes and references, report path/id failures visibly at boot, and do not duplicate simulation semantics. Verify an invalid or missing activity/season template is named and advancement is unavailable; the shipped tree passes the canonical `pnpm validate:content` gate.
- [x] 1.4 Implement `packages/shell/src/run.ts`: fixed development seed `week-shell-38-5` or explicit `?seed=`, root and season streams, generated level-1 roster from `tactical-shooter` and `western-europe`, quoted engagements, declared demo organization values and goal, and shipped `standard` calendar via public `@et/core` exports. Verify two reloads with the same seed yield identical opening run/season states, another seed changes generated state, a default no-seed load can advance an ordinary week through core, and `?seed=week-shell-38` correctly stays blocked at its week-0 contest. Reload explicitly signals that this is a new unsaved run.

## 2. Week command and state boundary

- [x] 2.1 Implement `packages/shell/src/session.ts` with tagged loading/editing/advancing/stopped/blocked/error modes, an orthogonal collapsed/expanded presentation state, an absolute-week draft of 4–6 weeks and activity/member selection restricted to unexecuted weeks. Test that toggling the sheet preserves the draft and committed run/RNG, a future assignment changes no committed numeric value, and an invalid plan advances no week.
- [x] 2.2 Route Continue only through `advanceSeason` from `@et/core`, with a synchronous single-flight guard and revision token invalidating stale handlers. Integration-test a real core block with rapid double activation: exactly one returned transition, no second recovery/debit/RNG move, and every displayed value sourced from the returned run.
- [x] 2.3 On a partial result, retain the returned stopped week and all stop reasons, freeze each returned `WeekResult`, shift the draft start to `result.runState.week`, preserve unexecuted assignments and append empty future weeks to a valid block; guard season boundary padding. Integration-test early stop after two weeks, immutable past rows, correct next-week continuation and byte-identical executed-week evidence after resumption.
- [x] 2.4 Block Continue for pending incident, uncovered engagement, `contest-ahead`, completed season and unsupported series without synthesizing any outcome; show exact reason/diagnostic and retain state on core rejection or unexpected result tag. Test these transitions using real core states where available and prove no state or RNG change on a rejected command.

## 3. Real DOM panel and placeholder scene

- [x] 3.1 Implement `packages/shell/src/week-panel.ts` and `src/style.css` as a collapsed current-state/reason summary with an expandable scrolling DOM planning sheet, a visible scene preview and one persistent Continue area; keep ordered activity/member controls, read-only executed-week evidence, slots, per-member energy/morale, core collective morale, balance and all returned reasons accessible. Browser-smoke opening the sheet, assigning by touch, closing/reopening without losing the draft, advancing once and seeing the committed scene/DOM summary with the sheet collapsed; compare numbers to returned core state and verify a stale old control cannot advance again.
- [x] 3.2 Implement `packages/shell/src/scene.ts` and `src/layout.ts` with PixiJS 8: one fixed 3/4 room, one labeled static placeholder figure per roster member from `packages/shell/placeholders/` (member *i* takes placeholder *i mod 5*), a uniform fractional fit of the 360×400 logical room at the device pixel ratio, `ResizeObserver` and a non-interactive canvas. A draft selection only highlights its named figure; an activity badge appears only after a committed result; never project resource values. Inspect narrow portrait, wide and landscape with sheet collapsed/expanded: recognizable figures when observing, a useful preview while editing, no hidden action/reason/safe-area conflict, plan scroll reaching the last row, minimum 44×44 CSS-pixel targets, and DOM duplicates of scene state.
- [x] 3.3 Make boot/content/core-command failures and Pixi initialization/ticker errors readable on the DOM without fabricating state or blanking the controls. Exercise each error path in a browser smoke (including a deliberate renderer failure), verify a rejected plan is correctable and a visual-only failure leaves the panel usable.

## 4. Layout evidence

- [x] 4.1 Capture 390×844 screenshots of the placeholder room for collapsed observation and expanded editing with the same seeded run. Record scene area, visible/occluded and recognizable people, draft vs committed cues, activity-edit difficulty/mis-taps and returned-consequence legibility in PR-2 review evidence; an illegible scene or unusable editor fails the check. Open the production build on at least one real phone and report touch editing and legibility as observation. Verify no Spine runtime, device profile or final-art contract entered #38 (those are #84).

## 5. Gate and archive on PR-2 only

- [x] 5.1 Run focused shell unit/integration tests and browser smoke through the real page; run `pnpm verify` including the new production build. Verify no shell imports core internals or duplicates energy/morale/money/advancement formulas, and no core or golden/baseline path changed.
- [ ] 5.2 Only on the implementation branch after PR-1 has merged, run `pnpm exec openspec archive week-shell -y`, check `openspec validate --all` and review the resulting `openspec/specs/game-shell/spec.md` diff; open PR-2 for human review without merging it.