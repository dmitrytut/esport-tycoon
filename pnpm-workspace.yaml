packages:
  - packages/*
  - tools/*

# pnpm 11+ forbids dependency build scripts by default (strictDepBuilds).
# esbuild unpacks a platform binary — without it vite and vitest don't work.
allowBuilds:
  esbuild: true
